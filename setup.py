#!/usr/bin/env python

from setuptools import setup, Extension

setup(name='Storm Image',
      version='1.0',
      description='This is the prediction of the weather',
      author='Team Ivan',
      packages=['tool'],
      package_dir={'tool': 'tool'}
      )
